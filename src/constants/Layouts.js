import {Dimensions} from 'react-native';

export const ROOT_HEADER_TITLE_SIZE = 20;
export const ROOT_HEADER_ICON_SIZE = 24;

export const CONTAINER_SIZE = 15;

export const {width, height} = Dimensions.get("screen");
